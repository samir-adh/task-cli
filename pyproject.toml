[project]
name = "task-cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.3.0",
]

[project.scripts]
task-cli = "main:task_cli"
